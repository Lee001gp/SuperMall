import React,{useEffect,useState} from 'react'; import { endpoints } from '../api/endpoints'; export default function StoreDirectoryPage({slug,onOpen}){ const [rows,setRows]=useState([]); useEffect(()=>{if(slug) endpoints.stores(slug).then(setRows)},[slug]); return <div><h2>Store Directory</h2>{rows.map(s=><div key={s.id}>{s.name}<button onClick={()=>onOpen(s)}>Open</button></div>)}</div>; }