/**
 * Deprecated module.
 * Kept only for backward compatibility imports in historical commits.
 * Runtime API no longer uses in-memory state; DB is source of truth via db.js.
 */
export const state = null;
