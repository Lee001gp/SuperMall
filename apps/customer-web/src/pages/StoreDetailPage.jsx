import React,{useEffect,useState} from 'react'; import { endpoints } from '../api/endpoints'; export default function StoreDetailPage({slug,storeSlug}){ const [d,setD]=useState(null); useEffect(()=>{ if(slug&&storeSlug) endpoints.store(slug,storeSlug).then(setD); },[slug,storeSlug]); if(!d) return <p>Loading store...</p>; return <div><h2>{d.name}</h2><p>{d.description}</p><p>Promotions: {d.promotions?.length||0}</p></div>; }